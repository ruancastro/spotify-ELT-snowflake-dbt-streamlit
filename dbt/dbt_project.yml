name: 'spotify_transform'
version: '1.0.0'

profile: 'spotify_transform'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  spotify_transform:
    bronze:
      +schema: BRONZE
    silver:
      +schema: SILVER
    gold:
      +schema: GOLD


